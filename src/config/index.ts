import connectColors from './connectColors';
import connectDB from './connectDB';
import connectSocket from './connectSocket';
import connectCors from './connectCors';
import connectWebPush from './connectWebPush';

export default {
  connectColors,
  connectDB,
  connectSocket,
  connectCors,
  connectWebPush,
}
