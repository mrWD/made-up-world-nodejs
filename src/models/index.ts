import User from './User';
import Page from './Page';
import Push, { Push as PushNotification } from './Push';
import Chat from './Chat';
import Message from './Message';

const models = {
  User,
  Page,
  Push,
  Chat,
  Message,
};

export {
  PushNotification,
};

export default models;
